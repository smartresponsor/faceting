doctrine:
  dbal:
    url: '%env(resolve:DATABASE_URL)%'
  orm:
    validate_xml_mapping: true
    naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
    auto_mapping: false
    mappings:
      Faceting:
        is_bundle: false
        type: attribute
        dir: '%kernel.project_dir%/src/Entity'
        prefix: 'App\Faceting\Entity'
        alias: Faceting

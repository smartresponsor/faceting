framework:
  secret: '%env(APP_SECRET)%'
  form: true
  csrf_protection: true
  router:
    utf8: true
  session:
    handler_id: null
    cookie_secure: auto
    cookie_samesite: lax
  validation:
    mapping:
      paths:
        - '%kernel.project_dir%/config/validator'

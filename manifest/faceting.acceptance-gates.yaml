manifest:
  id: faceting.acceptance_gates
  version: 1
  status: active

acceptance_gates:
  - only App namespace is used under src
  - no Domain directory exists
  - no service interface exists under src/Service
  - all component-local services use Faceting*Service naming
  - config files use faceting_ prefix
  - logs and reports are used to drive fixes
